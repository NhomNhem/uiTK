# Use Git LFS for large binary assets (prevents 100MB push failures on GitHub)
# After adding this file, run the following locally to convert existing history:
#   git lfs install
#   git add .gitattributes
#   git commit -m "chore: track large assets with Git LFS"
#   git lfs migrate import --include="*.psd"
#   git push origin --force --all
#   git push origin --force --tags

# Photoshop source files can exceed GitHub's 100MB limit (handle both cases)
*.psd filter=lfs diff=lfs merge=lfs -text
*.PSD filter=lfs diff=lfs merge=lfs -text
# Optionally add other heavy binary formats if your project uses them
*.psb filter=lfs diff=lfs merge=lfs -text
*.PSB filter=lfs diff=lfs merge=lfs -text
#*.tga filter=lfs diff=lfs merge=lfs -text
#*.fbx filter=lfs diff=lfs merge=lfs -text
#*.wav filter=lfs diff=lfs merge=lfs -text
#*.mp4 filter=lfs diff=lfs merge=lfs -text
